# ================================
# CONFIGURACIÓN DEL SERVIDOR
# ================================
PORT=5005
NODE_ENV=development

# ================================
# BASE DE DATOS POSTGRESQL
# ================================
# Host donde está corriendo PostgreSQL
DB_HOST=localhost

# Puerto de PostgreSQL (por defecto 5432)
DB_PORT=5432

# Nombre de la base de datos
DB_NAME=Fitlife

# Usuario de PostgreSQL
DB_USER=postgres

# Contraseña de PostgreSQL (CAMBIAR POR LA TUYA)
DB_PASSWORD=tu_password_aqui

# ================================
# CONFIGURACIÓN JWT
# ================================
# Clave secreta para firmar tokens JWT (¡CAMBIAR EN PRODUCCIÓN!)
# Genera una clave segura: https://generate-secret.vercel.app/32
JWT_SECRET=tu_jwt_secret_key_super_segura_aqui_cambiar_en_produccion

# Tiempo de expiración del token
# Opciones: 15m, 1h, 24h, 7d, etc.
JWT_EXPIRES_IN=120h

# ================================
# CONFIGURACIÓN CORS
# ================================
# URL del frontend permitida para CORS
# En desarrollo suele ser localhost:3000 o localhost:5173 (Vite)
FRONTEND_URL=http://localhost:5173

# ================================
# NOTAS IMPORTANTES:
# ================================
# 1. Copia este archivo como .env y configura tus valores
# 2. NUNCA subas el archivo .env al repositorio
# 3. En producción usa variables de entorno del servidor
# 4. Cambia JWT_SECRET por una clave segura en producción
# 5. Asegúrate que PostgreSQL esté corriendo antes de iniciar
# ================================
